# Frontend build-time config. Copy to .env if you need to override where
# the frontend looks for the optional AI backend (see /server).
# Defaults to http://localhost:8787 if unset.
VITE_AI_BACKEND_URL=http://localhost:8787
